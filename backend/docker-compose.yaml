services:
  db:
    image: postgres:17.6
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    container_name: workshop-hub-db
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
